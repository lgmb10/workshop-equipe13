const RUN = {
    spriteWidth: 288,
    itemWidth: 38,
    height: 32,
    spacing: 10,
    length: 6,
    image: new Image()
}

RUN.image.crossOrigin = true;
RUN.image.src = "images/player/run.png";

export default RUN;